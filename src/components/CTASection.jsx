"use client";
import { usePathname } from "next/navigation";
import Link from "next/link";
import { ArrowRight, ClipboardList } from "lucide-react";
export default function CTASection({city}){const pathname=usePathname();const parts=pathname.split("/").filter(Boolean);const district=city||(!["about","services","products","contact","items"].includes(parts[0])?parts[0]:"");const base=district?`/${district}`:"";return <section className="section-padding bg-white"><div className="container-custom"><div className="overflow-hidden rounded-[38px] bg-slate-950 p-9 text-white lg:p-16"><div className="grid items-center gap-10 lg:grid-cols-[1fr_auto]"><div><span className="inline-flex rounded-full border border-white/15 px-4 py-2 text-xs font-bold uppercase tracking-wider text-white/70">Have a product list?</span><h2 className="mt-5 max-w-3xl text-4xl font-black leading-tight lg:text-5xl">Turn a mixed biomedical requirement into a clear enquiry.</h2><p className="mt-5 max-w-2xl text-lg leading-8 text-white/65">Send the categories, quantities or specifications you already know. We can help organise the next step without limiting the request to one product type.</p></div><Link href={`${base}/contact`} className="inline-flex items-center justify-center gap-2 rounded-2xl bg-white px-7 py-4 font-bold text-slate-950">Start an enquiry <ArrowRight size={18}/></Link></div></div></div></section>}
